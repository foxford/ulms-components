import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Switcher } from './switcher'

<Meta title="atoms/Switcher" component={Switcher} />


### Switcher

<Canvas>
  <Story name="default" args={{}}>
    <Switcher />
  </Story>
</Canvas>


#### Active

<Canvas>
  <Story name="active" args={{}}>
    <Switcher on />
  </Story>
</Canvas>


#### With handler

export const state = { on: false }
export const Template = (state) => (
  <Switcher on={state.on} changeHandler={(e) => { console.log('toggle') }} />
)

<Canvas>
  <Story name="with handler" args={state}>
    {Template.bind({})}
  </Story>
</Canvas>
